# Native driver app (Expo — vars must start with EXPO_PUBLIC_ to reach the app)
EXPO_PUBLIC_API_URL="https://transit-ops-x-resonance-usye.vercel.app/api"
# Local dev against a machine on the same LAN (never localhost from a real phone):
# EXPO_PUBLIC_API_URL="http://192.168.x.x:5050/api"
